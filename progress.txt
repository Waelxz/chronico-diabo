Diabo · progress log
====================
(Append-only. Newest entry on top.)

----- 2026-05-14 00:21 UTC+01 — autonomous AFK hour, sprints 1+, 2, 3 -----

WHAT LANDED IN PROD
-------------------
- Sprint 1+ — resilience pass
   commit cd9c846 — `sprint 1+: serverless onFinish fix + OpenRouter fallback + chat history hydration`
   - `onFinish` in `/api/chat` made async + awaits the Mongo assistant write so Vercel doesn't kill the fn before the chat-doc updateOne lands.
   - OPENROUTER_FALLBACK_MODELS env var → OpenRouter's native `models[]` field.
   - GET /api/chats/current/messages — cookie-scoped history endpoint.
   - ChatPanel hydrates from server on mount with a cancellable fetch + 'Récupération…' state.

- Sprint 2 — empathy module
   commit 11011b5 — `sprint 2: empathy module - sentiment via HF drives Diabo's face`
   - Migrated to `@huggingface/inference` SDK (legacy api-inference.huggingface.co was deprecated).
   - /api/emotion → cardiffnlp/twitter-xlm-roberta-base-sentiment via 'hf-inference' provider.
   - emotion-map.ts: positive→happy, neutral→neutral, negative→worried (empathetic mirroring; sad user → concerned Diabo, not sad Diabo).
   - DiaboProvider.applyEmotion + DiaboStage now drives 4 numeric Rive props (mouthState, eyebrowMood, eyelidState, happiness) on top of the existing booleans.
   - ChatPanel fires /api/emotion in parallel with sendMessage on every submit.

- Sprint 3 — RAG over diabetes KB
   commit 31c1699 — `sprint 3: RAG over diabetes knowledge base (Atlas Vector Search)`
   - HF featureExtraction with sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (384 dim).
   - kb_chunks collection in Atlas with vector index `kb_chunks_vector` (cosine, 384 dim, topic filter).
   - 14 curated FR seed chunks (src/lib/diabo/knowledge.ts) covering concept, food, activity, monitoring, hypo, hyper, mental, travel, ramadan, pregnancy. Maghreb-aware, no specific medical numbers, no copy-paste from copyrighted sources.
   - POST /api/admin/seed-kb — idempotent ingestion route, disabled in prod by default (NODE_ENV check).
   - /api/chat retrieves top-3 chunks via cosine vector search, prepends as 'Contexte issu de la base de connaissances Diabo' to the persona system prompt. Best-effort: KB outage → persona-only.

PROD VERIFICATIONS (Vercel: chronico-diabo.vercel.app)
------------------------------------------------------
- `dpl_SujLeHhWMoUarv7A6Q2MtQujR5Qa` deploy READY in 27s.
- /api/chat with hypo question → empathetic FR answer with all 7 hypo signs (matches seed chunk), the 15-min sugar rule, and emergency numbers 190/14/141 (matches persona). 27s end-to-end on gpt-oss-120b:free.
- /api/emotion with "Je suis triste aujourd'hui" → negative (0.946). Warm <500ms.
- Mongo chat doc `6a0508520bfd6feee11bee2d`: `lastUserMessage` + `lastAssistantMessage` both populated, updatedAt 25s after createdAt → onFinish fix works on serverless.

PROVIDER ROTATION SAGA
----------------------
Hit Venice rate-limits on `qwen3-next-80b`, `hermes-3-405b`, `llama-3.3-70b` mid-session (free key shared quota).
Settled on:
   primary  : openai/gpt-oss-120b:free          (OpenInference provider — different from Venice)
   fallback : google/gemma-4-26b-a4b-it:free   (Google AI Studio)
              qwen/qwen3-next-80b-a3b-instruct:free
              meta-llama/llama-3.3-70b-instruct:free
Cross-provider rotation matters more than cross-model on free tier.

KNOWN ISSUES (non-blocking)
---------------------------
- Local dev sometimes returns 500 on /api/chat when the augmented system prompt is large AND the fallback chain happens to land on a Venice-rate-limited provider. Prod is currently happier than local because Vercel's IP gets a fresh provider lottery. If it bites again locally, swap the .env primary to gemma or wait ~30s.
- `progress.txt` line count is growing — fine for now, archive in a sprint when convenient.

VERCEL ENV VARS — STILL UNSET (optional)
----------------------------------------
- `OPENROUTER_FALLBACK_MODELS` — Vercel uses the zod default (empty string = no fallback). To enable native fallback in prod too, paste:
   OPENROUTER_FALLBACK_MODELS=google/gemma-4-26b-a4b-it:free,qwen/qwen3-next-80b-a3b-instruct:free,meta-llama/llama-3.3-70b-instruct:free
- `APP_URL` — set to `https://chronico-diabo.vercel.app` once you're ready (OpenRouter analytics referer only).

SUGGESTED NEXT MOVES
--------------------
- Sprint 4: restaurants module (Overpass + LLM scoring + Leaflet map of diabetes-friendly places near user).
- Optional polish: surface KB citations in the chat UI ("Diabo a consulté: Signes d'hypoglycémie / Aliments à modérer"). Currently the persona is told NOT to cite — flip that for transparency.
- Optional polish: re-write the smoke-chat script to retry on 500 (since OpenRouter free-tier flakiness is structural).

ATLAS STATE AT END OF SESSION
-----------------------------
chronico_diabo.chats        : multiple cookie sessions; lastAssistantMessage populated since sprint 1+ fix
chronico_diabo.messages     : ~12-15 docs (assorted smoke tests)
chronico_diabo.kb_chunks    : 14 docs, all with 384-dim embeddings
                              vector index `kb_chunks_vector` (READY, queryable)



----- 2026-05-13 22:10 UTC+01 — autonomous sprint 1, while user was AFK -----

DONE this run
-------------
1. Installed Vercel AI SDK v6 + @ai-sdk/react v3 + @openrouter/ai-sdk-provider v2.
2. Created the LLM provider helper:           src/lib/llm.ts
3. Created the Diabo system prompt (FR):      src/lib/diabo/persona.ts
4. Created the Mongo persistence helpers:     src/lib/db/chats.ts
5. Created the streaming chat route:          src/app/api/chat/route.ts
   - Node runtime (mongodb driver doesn't run on Edge).
   - httpOnly cookie `diabo_chat_id`, minted on first POST, 30-day Max-Age.
   - Calls touchChat() + appendMessage(user) fire-and-forget BEFORE the stream.
   - onFinish hook persists the assistant turn.
   - Never blocks the stream on Mongo (try/catch on every write).
6. Created the React context for the avatar:  src/components/diabo/DiaboProvider.tsx
   - useDiabo() exposes setIsThinking / setIsTalking / setPreset / patch.
   - Only sanctioned channel between React and Diabo per AGENTS.md.
7. Refactored DiaboStage to bind ViewModel:   src/components/diabo/DiaboStage.tsx
   - useViewModelInstanceBoolean('isTalking' / 'isThinking', rive.viewModelInstance)
   - Pushes context state into Rive on every effect tick.
8. Built the ChatPanel UI:                    src/components/chat/ChatPanel.tsx
   - useChat({ transport: DefaultChatTransport }), FR copy.
   - Drives Diabo lifecycle from status: submitted → thinking, streaming → talking.
   - Stop button while streaming, Regenerate button after.
   - Empty-state, typing-dots, error bubble.
9. Rebuilt the landing page:                  src/app/page.tsx
   - Wraps everything in <DiaboProvider>, side-by-side avatar + chat on lg+.
10. Created Mongo collections + indexes via the MongoDB MCP:
    db: chronico_diabo
    chats:    { _id, createdAt, updatedAt, lastUserMessage?, lastAssistantMessage? }
              index: updatedAt_desc
    messages: { _id, chatId, role, content, createdAt }
              index: chatId_createdAt
11. Smoke-tested via scripts/smoke-chat.mjs   (commit-included tool):
    User: "Bonjour Diabo, je suis fatiguée aujourd'hui…"
    Diabo: empathetic FR reply with food/walk advice + medical disclaimer.
    237 SSE chunks, ~26s on openai/gpt-oss-120b:free.
12. Visually verified via Playwright at http://localhost:3000:
    - Avatar canvas 448×448 painted.
    - Chat input → Diabo replies → conversation persisted to Mongo.
    - 0 console errors, 0 warnings.
13. Pushed:
    commit 026e19e — "sprint 1: streaming chat with Diabo persona + Mongo persistence"
    14 files changed, 906 insertions(+), 40 deletions(-)
14. Updated README.md sprint table (0 done, 1 done, 2 next).

Model rotation note
-------------------
- Started on meta-llama/llama-3.3-70b-instruct:free → upstream rate-limit (Venice provider).
- Tried google/gemini-2.0-flash-exp:free → "no endpoints found" (gone from OpenRouter).
- Settled on openai/gpt-oss-120b:free → works, sounds great in French, OpenAI-lineage.
- Free model menu in .env.example: gpt-oss-120b > qwen3-next-80b > llama-3.3-70b > hermes-3-405b > glm-4.5-air.

Open follow-ups (for next sessions)
-----------------------------------
- [ ] Production build verification (build was kicked off in background, see build.log).
- [ ] Sprint 2: emotion detection.
      Plan: /api/emotion route → HF Inference API (cardiffnlp/twitter-xlm-roberta-base-sentiment).
      On chat finish, post the user's text to /api/emotion, map result to DIABO_PRESETS via a lookup.
      Update DiaboProvider with `applyEmotion(result)`.
- [ ] Multi-model fallback in /api/chat: catch 429 from streamText (currently logs but client sees error event); retry with next model in OPENROUTER_MODELS list before giving up.
- [ ] Persist client cookie chat history into the UI on reload (currently messages list is in-memory).
- [ ] Add /api/chats/[id]/messages GET to hydrate ChatPanel from Mongo.
- [ ] Vercel preview deploy (still pending — needs human in the Vercel dashboard for env vars).
- [ ] Replace the temporary `dev-server.log` mechanism with a proper background dev runner.

Dev server (during my run)
--------------------------
- Launched via `Start-Process powershell -ArgumentList "-c","npm run dev *>&1 | Tee-Object dev-server.log" -WindowStyle Hidden`.
- It may still be running on port 3000 (PID was 8996 when I last checked).
- If you don't want it, kill it with:
    Get-NetTCPConnection -LocalPort 3000 -State Listen | % { Stop-Process -Id $_.OwningProcess -Force }

Mongo Atlas state at end of run
-------------------------------
chronico_diabo.chats:    3 docs (one per smoke-test session)
chronico_diabo.messages: 4 docs (3 user, 1 assistant)
indexes:                 chatId_createdAt, updatedAt_desc
